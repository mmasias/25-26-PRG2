Dylan Ruiz:
para resolver este trabajo decidí con ciclos, especialmente con el switch para crear
cada función que se esta solicitando como LineaActiva,EditarLinea,etc
luego cada opcopn sera una función que sera llamada por el switch en base a la función
que el usuario solicite, el usuario digitara una letra que llamara las funciones que el usuario solicite, de esta manera se resolvera el problema
